#!/bin/bash
# Spawns concurrent background transactions

NUM_BUYERS=20

echo "Spawning $NUM_BUYERS parallel purchase requests..."

for ((i=1; i<=NUM_BUYERS; i++)); do
    # Run in the background
    ./buy_product & 
done

# Wait for all background processes to finish before returning terminal control
wait 
echo "Swarm complete."
